#include <NixRenderer.h>

namespace Nix {
	class UITextureManager {
	private:
		ITexture*		m_texture;
		uint32_t 		m_numImageLayer;
		uint32_t		m_numFontLayer;
	public:
		UITextureManager() {

		}
	};
}